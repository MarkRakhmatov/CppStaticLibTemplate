name: autotag
on:
  push:
    branches:
      - main

jobs:
  tag:
    permissions:
      # permission required to create tag
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: ./.github/tag
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
